# =====================================================
# Task Manager Frontend - Environment Variables Template
# =====================================================
# Copy this file to .env for local development
# or .env.production for production deployment
# =====================================================

# =====================================================
# API CONFIGURATION
# =====================================================

# Backend API base URL
# Local development: http://localhost:8080/api
# Production (Render): https://your-backend-name.onrender.com/api
VITE_API_BASE_URL=http://localhost:8080/api

# =====================================================
# EXAMPLES FOR DIFFERENT ENVIRONMENTS
# =====================================================
#
# Local Development (.env):
# VITE_API_BASE_URL=http://localhost:8080/api
#
# Production on Vercel (.env.production):
# VITE_API_BASE_URL=https://task-manager-backend.onrender.com/api
#
# Staging Environment:
# VITE_API_BASE_URL=https://task-manager-backend-staging.onrender.com/api
#
# =====================================================
# QUICK START
# =====================================================
#
# 1. For local development:
#    cp .env.example .env
#    (Keep the default localhost URL)
#
# 2. For production deployment:
#    Create .env.production with your Render backend URL
#    (This file is gitignored for security)
#
# 3. Start development server:
#    npm run dev
#
# 4. Build for production:
#    npm run build
#    (Uses .env.production if it exists)
#
# =====================================================
# IMPORTANT NOTES
# =====================================================
#
# - All env vars MUST start with VITE_ prefix to be
#   accessible in the application
#
# - Environment variables are embedded at build time,
#   not runtime
#
# - .env.production is gitignored - safe for secrets
#   (though Vite exposes them to client anyway)
#
# - For Vercel deployment, set environment variables
#   in the Vercel dashboard instead of .env.production
#
# =====================================================
